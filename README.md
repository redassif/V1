# V1
This project implements a complete reinforcement learning framework for the Needle-in-Sequence environment, defined as a finite episodic MDP where the state consists of the token sequence and a hidden "needle" index in a hierarchical approach towards the attention mechanism, it uses Blocks in a hierarchy.
